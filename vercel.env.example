# Vercel environment variable examples for this project
# Do NOT commit real secrets. Use the Vercel Dashboard or CLI to set them.

# Required for the serverless Gemini function (api/gemini.ts)
GEMINI_API_KEY=your-real-gemini-api-key-here

# Optional fallback name to use when calling the API locally
DEFAULT_BROTHER_NAME=Krivan
